<%#= f.fields_for :percentages, vendor.percentages.find_or_initialize_by(product: product) do |p| -%>
  <%#= p.text_area :percentage %>
  <%#= p.hidden_field :product_id, value: product.id %>
<%# end -%>

<fieldset>
  <%= f.label :name, "Percentage" %>
  <%= f.text_area :percentage %>
</fieldset>